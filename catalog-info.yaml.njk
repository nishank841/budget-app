apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  links:
    - url: "${{ values.confluenceUrl }}"
      title: "Documentation"
      icon: docs
spec:
  type: service
  owner: user:guest
