apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  annotations:
    backstage.io/view-url: ${{ values.confluenceUrl }}
  links:
    - url: ${{ values.confluenceUrl }}
      title: "Documentation"
      icon: docs
spec:
  type: service
  lifecycle: experimental
  owner: user:guest
