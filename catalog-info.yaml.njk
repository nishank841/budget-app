apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  annotations:
    backstage.io/documentation-url: ${{ values.confluenceUrl }}
spec:
  type: service
  owner: group:default/guests
